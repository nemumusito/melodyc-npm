{
  "name": "melodyc-npm",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "npx serve . --ssl-cert ./cert.pem --ssl-key ./key.pem",
    "dev:setup": "openssl req -x509 -newkey rsa:2048 -keyout key.pem -out cert.pem -days 365 -nodes -subj '/CN=localhost'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "tone": "^14.7.77",
    "webmidi": "^3.1.7",
    "soundfont-player": "^0.12.0"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
